{
  "name": "placementsio-coding-exercise",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "cd client && npm install && cd ../server && npm install",
    "start": "npm run server",
    "server": "cd server && npm start",
    "client": "cd client && npm start"
  }
}
